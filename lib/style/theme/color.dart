import 'package:flutter/material.dart';

class AppColors {
  // Background color - light beige village feel
  static const background = Color(0xFFE8F5E9);

  // Primary warm brown for sender messages, buttons, app bars
  static const primary = Color(0xFF2E7D32);

  // Secondary soft green for receiver messages, highlights
  static const complementary = Color(0xFF4FC3F7);

  // Dark warm gray for text
  static const textDark = Color(0xFF333333);

  // Warm orange accent for FABs, active buttons
  static const accent = Color(0xFFFF7043);

  // Online status indicator - fresh green (can keep as is or soften)
  static const onlineStatus = Color(0xFF4CAF50); // olive green

  // Notifications - keep bold red for alerts
  static const notifications = Color(0xFFD32F2F);

  // Bubble shadow - soft warm gray
  static const bubbleShadow = Color(0xFFCFD8DC);

  // Danger
  static const dangerSoft = Color(0xFFFFEBEE);
  static const danger = Color(0xFFC62828);
  static const dangerDark = Color(0xFFB71C1C);

  // Suggested success colors (muted, natural greens)
  // Success
  static const successSoft = Color(0xFFE8F5E9);
  static const success = Color(0xFF43A047);
  static const successDark = Color(0xFF2E7D32);

  // Info
  static const infoSoft = Color(0xFFE1F5FE);
  static const info = Color(0xFF29B6F6);
  static const infoDark = Color(0xFF0288D1);
}
